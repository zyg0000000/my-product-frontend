# Performance 模块化架构文档

## 📁 模块结构

已成功将 `performance.js` (950行) 拆分为 11 个独立模块，总计 1,803 行。

### 模块文件列表及行数

1. **constants.js** (88行) - 常量定义
2. **utils.js** (73行) - 通用工具函数
3. **api.js** (73行) - API 请求封装
4. **state-manager.js** (110行) - 状态管理
5. **table-renderer.js** (158行) - 表格渲染
6. **filter-panel.js** (247行) - 筛选面板
7. **dashboard.js** (119行) - 仪表板统计
8. **pagination.js** (137行) - 分页功能
9. **import-export.js** (277行) - 导入导出
10. **modal-dimensions.js** (198行) - 维度管理模态框
11. **main.js** (323行) - 主入口文件

## 🔗 模块依赖关系

```
main.js
├── constants.js
├── api.js
│   ├── constants.js
│   └── utils.js
├── utils.js
├── state-manager.js
│   └── constants.js
├── table-renderer.js
│   ├── state-manager.js
│   ├── utils.js
│   └── pagination.js
├── filter-panel.js
│   ├── state-manager.js
│   └── utils.js
├── dashboard.js
│   └── state-manager.js
├── pagination.js
│   ├── state-manager.js
│   └── constants.js
├── import-export.js
│   ├── constants.js
│   ├── api.js
│   ├── utils.js
│   ├── state-manager.js
│   └── filter-panel.js
└── modal-dimensions.js
    ├── state-manager.js
    └── table-renderer.js
```

## 📝 模块职责说明

### 1. **constants.js**
- API 端点配置
- localStorage 键名定义
- 预设维度配置 (PRESET_DIMENSIONS)
- Excel 列映射 (COLUMN_MAP)

### 2. **utils.js**
- 自定义 Alert 提示框管理
- 日期格式化函数
- 百分比格式化函数
- 空值处理函数

### 3. **api.js**
- 统一的 API 请求封装
- 错误处理逻辑
- 请求响应处理

### 4. **state-manager.js**
- 全局状态变量管理
- 维度配置的加载和保存
- 筛选器状态管理
- 分页和排序状态

### 5. **table-renderer.js**
- 数据表格渲染
- 单元格格式化
- 排序功能处理
- 表头和表体生成

### 6. **filter-panel.js**
- 筛选器复选框渲染
- 数据筛选行管理
- 搜索参数构建
- 筛选器事件处理

### 7. **dashboard.js**
- 统计卡片渲染
- 层级分布显示
- CPM 分布显示
- 受众分布图表

### 8. **pagination.js**
- 分页控件渲染
- 页码按钮生成
- 分页事件处理
- 每页条数管理

### 9. **import-export.js**
- Excel 文件导入处理
- 飞书数据同步
- 数据导出功能
- 批量更新处理

### 10. **modal-dimensions.js**
- 维度选择界面
- 拖拽排序功能
- 维度添加/删除
- 配置保存处理

### 11. **main.js**
- 应用初始化
- 事件监听器设置
- 模块协调
- 数据获取和刷新

## 🎯 关键特性

1. **ES6 模块化**: 所有模块使用 ES6 import/export 语法
2. **单一职责**: 每个模块负责特定功能领域
3. **低耦合**: 模块间通过明确的接口通信
4. **状态集中管理**: 通过 state-manager.js 统一管理应用状态
5. **可维护性**: 清晰的代码组织和详细的注释

## 🚀 使用说明

在 HTML 文件中引入主模块：

```html
<script type="module" src="/performance/main.js"></script>
```

确保服务器正确配置 MIME 类型以支持 ES6 模块。

## ⚠️ 注意事项

1. **XLSX 依赖**: 导入导出功能依赖全局 XLSX 对象，需要确保在页面中引入 SheetJS 库
2. **Sortable 依赖**: 维度排序功能依赖全局 Sortable 对象，需要引入 Sortable.js 库
3. **DOM 依赖**: 各模块依赖特定的 DOM 元素 ID，确保 HTML 结构正确
4. **浏览器兼容性**: 使用 ES6 模块需要现代浏览器支持

## 🔄 迁移检查清单

- [x] API 配置和常量定义
- [x] 工具函数提取
- [x] 状态管理集中化
- [x] UI 渲染模块化
- [x] 事件处理分离
- [x] 导入导出功能独立
- [x] 所有功能测试点保留
- [x] 代码注释和文档完善