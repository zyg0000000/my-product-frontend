# AgentWorks - 广告代理项目管理平台 (v2.0)

> **新一代多平台广告代理项目管理平台**

---

## 📊 产品信息

- **产品名称**: AgentWorks
- **中文名称**: 广告代理项目管理平台
- **版本**: v2.0
- **状态**: 🚧 开发中
- **关联数据库**: `agentworks_db`

---

## 🚀 核心特性

### ✨ 多平台支持
- 支持抖音、小红书、B站、快手等多个平台
- 统一的达人管理界面
- 跨平台数据对比和分析

### 🔗 智能达人管理
- **oneId 系统**：跨平台达人统一标识
- **后期合并**：支持发现重复达人后合并
- **历史追溯**：完整的数据变更历史
- **智能推荐**：自动识别可能重复的达人

### 📈 项目管理升级
- 支持多平台项目
- 灵活的合作模式
- 实时数据监控
- 效果对比分析

### 🎨 现代化界面
- 响应式设计
- 直观的用户体验
- 高性能渲染

---

## 📁 项目结构

```
agentworks/
├── README.md              # 本文件
├── package.json           # 依赖管理（可选）
│
├── public/                # 静态资源
│   ├── index.html         # 入口页面
│   └── assets/            # 图片、字体等
│
├── src/                   # 源代码
│   ├── pages/             # 页面组件
│   │   ├── TalentList/    # 达人列表（多平台）
│   │   ├── TalentDetail/  # 达人详情
│   │   ├── ProjectList/   # 项目列表
│   │   ├── CooperationManagement/  # 合作管理
│   │   └── ...
│   │
│   ├── components/        # 通用组件
│   │   ├── TalentCard/    # 达人卡片
│   │   ├── PlatformTag/   # 平台标签
│   │   ├── MergeModal/    # 合并达人弹窗
│   │   └── ...
│   │
│   ├── api/               # API 调用层
│   │   ├── talent.js      # 达人相关 API
│   │   ├── project.js     # 项目相关 API
│   │   ├── cooperation.js # 合作相关 API
│   │   └── config.js      # API 配置
│   │
│   ├── utils/             # 工具函数
│   │   ├── formatters.js  # 格式化工具
│   │   ├── validators.js  # 验证工具
│   │   └── helpers.js     # 辅助函数
│   │
│   └── styles/            # 样式文件
│       ├── global.css     # 全局样式
│       └── variables.css  # CSS 变量
│
└── docs/                  # 产品文档
    ├── DESIGN.md          # 设计文档
    ├── FEATURES.md        # 功能清单
    └── API.md             # API 对接文档
```

---

## 🛠️ 技术栈选择

### 选项 1：纯原生（和 byteproject 一致）
```
- HTML5 + ES6 Modules
- Tailwind CSS (CDN)
- 无构建工具
```
**优点**：
- ✅ 快速开发，即改即看
- ✅ 部署简单（Cloudflare Pages 直接托管）
- ✅ 和旧前端技术栈一致

**缺点**：
- ⚠️ 无类型检查
- ⚠️ 代码组织依赖规范

---

### 选项 2：现代框架（推荐）
```
- React / Vue / Svelte
- TypeScript
- Vite + Tailwind CSS
- 组件化开发
```
**优点**：
- ✅ 类型安全（TypeScript）
- ✅ 组件复用性强
- ✅ 开发效率高
- ✅ 易于维护和扩展

**缺点**：
- ⚠️ 需要构建步骤
- ⚠️ 学习曲线（如果团队不熟悉）

---

## 🚀 快速开始

### 环境要求

- 现代浏览器（Chrome、Firefox、Safari、Edge）
- Node.js 18+（如果使用现代框架）
- Git

### 开发流程

**选项 1（纯原生）**：
```bash
# 1. 进入目录
cd frontends/agentworks/public/

# 2. 启动本地服务器
python -m http.server 8000
# 或
npx http-server -p 8000

# 3. 打开浏览器
# http://localhost:8000/index.html
```

**选项 2（现代框架）**：
```bash
# 1. 进入目录
cd frontends/agentworks/

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 打开浏览器（自动打开）
# http://localhost:5173
```

---

## 📖 核心功能模块

### 1. 达人管理（多平台）

**功能**：
- 达人列表（按 oneId 分组展示）
- 新增达人（支持关联已有达人）
- 编辑达人信息
- 合并重复达人
- 智能推荐重复达人
- 查看合并历史

**页面**：`src/pages/TalentList/`

---

### 2. 项目管理

**功能**：
- 创建多平台项目
- 项目配置和预算管理
- 项目执行跟踪
- 跨平台数据对比

**页面**：`src/pages/ProjectList/`

---

### 3. 合作管理

**功能**：
- 创建合作订单（支持多平台）
- 合作进度追踪
- 效果数据录入
- 财务结算

**页面**：`src/pages/CooperationManagement/`

---

### 4. 数据分析

**功能**：
- 项目效果分析
- 达人效果对比
- 平台数据对比
- ROI 计算

**页面**：`src/pages/DataAnalysis/`

---

## 🔌 API 对接

### API 配置

```javascript
// src/api/config.js
export const API_CONFIG = {
  baseURL: 'https://sd2pl0r2pkvfku8btbid0.apigateway-cn-shanghai.volceapi.com',
  dbVersion: 'v2',           // 使用 v2 数据库
  timeout: 30000
};
```

### API 示例

```javascript
// src/api/talent.js
import { API_CONFIG } from './config.js';

// 获取达人列表（按 oneId 分组）
export async function getTalents(filters = {}) {
  const response = await fetch(`${API_CONFIG.baseURL}/getTalents`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      dbVersion: 'v2',
      groupBy: 'oneId',
      ...filters
    })
  });
  return response.json();
}

// 创建达人（支持关联）
export async function createTalent(talentData) {
  const response = await fetch(`${API_CONFIG.baseURL}/createTalent`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      dbVersion: 'v2',
      ...talentData
    })
  });
  return response.json();
}

// 合并达人
export async function mergeTalents(primaryOneId, mergedOneId, reason) {
  const response = await fetch(`${API_CONFIG.baseURL}/mergeTalents`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      dbVersion: 'v2',
      primaryOneId,
      mergedOneId,
      reason
    })
  });
  return response.json();
}
```

---

## 📚 相关文档

| 文档 | 说明 | 路径 |
|------|------|------|
| 设计文档 | 产品设计和交互说明 | `docs/DESIGN.md` |
| 功能清单 | 完整功能列表 | `docs/FEATURES.md` |
| API 文档 | 后端 API 对接说明 | `docs/API.md` |
| 数据库设计 | v2 数据库架构 | `../../database/agentworks_db/` |

---

## 🔗 相关链接

- **数据库定义**：`../../database/agentworks_db/`
- **云函数代码**：`../../functions/`
- **旧产品（参考）**：`../byteproject/`
- **项目文档**：`../../docs/`

---

## 🛠️ 开发计划

### ✅ Phase 1: 项目初始化（已完成）
- [x] 创建目录结构
- [x] 编写 README 文档

### 🚧 Phase 2: 技术选型（进行中）
- [ ] 确定技术栈（纯原生 vs 现代框架）
- [ ] 搭建基础框架
- [ ] 配置开发环境

### 📅 Phase 3: 核心功能开发（待开始）
- [ ] 达人管理模块
- [ ] 项目管理模块
- [ ] 合作管理模块
- [ ] 数据分析模块

### 📅 Phase 4: 测试和上线（待开始）
- [ ] 功能测试
- [ ] 性能优化
- [ ] 部署到 Cloudflare Pages
- [ ] 正式上线

---

## ⚠️ 重要提示

1. **和 1.0 产品完全独立**，互不影响
2. **使用 v2 数据库** (`agentworks_db`)
3. **所有 API 请求需带 `dbVersion: 'v2'` 参数**
4. **遵循 oneId 设计逻辑**（详见数据库设计文档）

---

**产品负责人**：产品团队
**最后更新**：2025-11-11
**版本**：v2.0 (In Development 🚧)
