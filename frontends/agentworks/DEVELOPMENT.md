# AgentWorks 开发指南

## 🚀 快速开始

### 环境准备

1. **Node.js**: 需要 Node.js 20+
2. **Git**: 用于版本控制
3. **VS Code**: 推荐使用（有良好的 Git 集成）

### 首次设置

```bash
# 1. 克隆仓库
git clone https://github.com/zyg0000000/my-product-frontend.git
cd my-product-frontend

# 2. 进入 AgentWorks 目录
cd frontends/agentworks

# 3. 安装依赖
npm install

# 4. 启动开发服务器
npm run dev

# 5. 访问应用
# 打开浏览器访问 http://localhost:5173/
```

## 💻 本地开发流程

### 1. 启动开发环境

```bash
cd frontends/agentworks
npm run dev
```

特点：
- ✅ 热更新（HMR）：代码更改自动刷新
- ✅ TypeScript 类型检查
- ✅ 实时错误提示
- ✅ 开发服务器端口：5173

### 2. 开发工作流

#### 日常开发循环
```
1. 修改代码
   ↓
2. Vite 检测文件变化
   ↓
3. 浏览器自动刷新（<1秒）
   ↓
4. 查看效果
   ↓
5. 继续迭代
```

#### 调试技巧
- 使用浏览器开发者工具（F12）
- 查看控制台日志
- 监控网络请求
- React DevTools 调试组件

### 3. 代码提交流程

#### 方式一：命令行

```bash
# 查看更改
git status

# 添加更改
git add .

# 提交（使用语义化提交信息）
git commit -m "feat: 添加新功能描述"

# 推送到 GitHub
git push origin main
```

#### 方式二：VS Code GUI（推荐）

1. 打开源代码管理面板
   - 快捷键：`Ctrl+Shift+G`（Windows/Linux）
   - 快捷键：`Cmd+Shift+G`（Mac）

2. 查看文件更改
   - 点击文件查看 diff
   - 可选择性暂存文件

3. 提交更改
   - 输入提交信息
   - 按 `Ctrl/Cmd + Enter` 提交

4. 同步到 GitHub
   - 点击状态栏的同步按钮
   - 或点击 "..." → "推送"

## 📝 提交信息规范

使用语义化提交信息，格式：`<type>: <description>`

### 类型说明

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat: 添加达人批量导入功能` |
| `fix` | Bug 修复 | `fix: 修复返点显示错误` |
| `docs` | 文档更新 | `docs: 更新 API 文档` |
| `style` | 代码格式/样式 | `style: 调整按钮样式` |
| `refactor` | 代码重构 | `refactor: 重构 API 调用逻辑` |
| `test` | 测试相关 | `test: 添加单元测试` |
| `chore` | 构建/工具 | `chore: 更新依赖版本` |

### 提交信息示例

```bash
# 单行描述
git commit -m "feat: 在达人列表中显示 currentRebate 字段"

# 多行描述（推荐）
git commit -m "feat: 在达人列表中显示 currentRebate 字段

- 添加 currentRebate 对象类型定义
- 修改列表页面显示 currentRebate.rate 值
- 格式化返点率保留 2 位小数
- 更新 getTalents 云函数返回数据"
```

## 🏗️ 项目结构

```
frontends/agentworks/
├── src/
│   ├── pages/              # 页面组件
│   │   ├── Home/           # 首页
│   │   ├── Talents/        # 达人管理
│   │   └── TalentDetail/   # 达人详情
│   ├── components/         # 可复用组件
│   │   ├── Layout/         # 布局组件
│   │   ├── Sidebar/        # 侧边栏
│   │   └── Modals/         # 弹窗组件
│   ├── api/                # API 调用
│   │   ├── client.ts       # HTTP 客户端
│   │   ├── talent.ts       # 达人 API
│   │   └── rebate.ts       # 返点 API
│   ├── types/              # TypeScript 类型
│   │   ├── talent.ts       # 达人类型定义
│   │   └── rebate.ts       # 返点类型定义
│   └── utils/              # 工具函数
│       └── formatters.ts   # 格式化函数
├── public/                 # 静态资源
├── package.json           # 项目配置
└── vite.config.ts         # Vite 配置
```

## 🔧 常用命令

```bash
# 开发
npm run dev              # 启动开发服务器
npm run build           # 构建生产版本
npm run preview         # 预览生产构建

# 代码质量
npm run lint            # 运行 ESLint
npm run format          # 格式化代码（Prettier）

# Git 操作
git status              # 查看状态
git diff               # 查看更改
git log --oneline -5   # 查看最近提交
git branch             # 查看分支
```

## 🚢 部署流程

### 前端部署（自动）

1. 推送到 GitHub main 分支
2. Cloudflare Pages 自动检测
3. 自动构建和部署（1-2 分钟）
4. 访问生产环境

### 云函数部署（手动）

1. 修改 `functions/` 目录下的云函数
2. 登录火山引擎控制台
3. 更新对应的云函数代码
4. 测试并发布

## 🐛 故障排除

### 常见问题

#### 1. 端口被占用
```bash
# 错误：Port 5173 is already in use
# 解决方案：使用其他端口
npm run dev -- --port 3000
```

#### 2. 依赖安装失败
```bash
# 清理并重新安装
rm -rf node_modules package-lock.json
npm install
```

#### 3. TypeScript 错误
```bash
# 检查类型错误
npm run build
# 查看具体错误信息并修复
```

#### 4. Git 冲突
```bash
# 拉取最新代码
git pull origin main
# 解决冲突后
git add .
git commit -m "fix: 解决合并冲突"
git push
```

## 📚 开发最佳实践

### 代码规范
- ✅ 使用 TypeScript 类型
- ✅ 组件使用函数式组件 + Hooks
- ✅ 统一使用 Tailwind CSS
- ✅ 遵循 ESLint 规则

### 性能优化
- ✅ 使用 React.memo 避免不必要的重渲染
- ✅ 大列表使用虚拟滚动
- ✅ 图片懒加载
- ✅ 代码分割和懒加载

### 安全性
- ✅ 避免 XSS 攻击
- ✅ API 调用使用 HTTPS
- ✅ 敏感信息不要硬编码
- ✅ 输入验证和清理

## 📞 获取帮助

- 查看 [项目文档](./PROJECT_SUMMARY.md)
- 查看 [更新日志](./CHANGELOG.md)
- 提交 [GitHub Issue](https://github.com/zyg0000000/my-product-frontend/issues)

---

*最后更新：2025-11-16*