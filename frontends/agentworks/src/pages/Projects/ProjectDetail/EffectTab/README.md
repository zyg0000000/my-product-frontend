# EffectTab (v4.0.0) - 效果验收 Tab

**全新设计** - 融合 ByteProject 的数据看板理念与 AgentWorks 的深邃靛蓝美学

---

## 🎨 设计理念

### 视觉方向: 玻璃态赛博朋克 (Glassmorphism meets Cyberpunk)

打破传统数据看板的平淡无奇，采用大胆的设计语言:

- **玻璃态卡片**: 半透明背景 + backdrop-filter 模糊效果,营造空间层次感
- **动态光影**: 渐变光晕、悬停光效、脉冲动画,让数据"活"起来
- **霓虹强调色**: 使用鲜明的渐变色系区分不同数据状态
- **金属质感**: 顶部彩色条采用金属渐变,增强视觉冲击力
- **流体动画**: 所有交互都有流畅的缓动函数,打造愉悦的操作体验

**设计目标**: 让用户在查看效果数据时感受到"wow"时刻,而不是枯燥的表格堆砌

---

## 📁 文件结构

```
EffectTab/
├── index.tsx                    # 模块导出
├── EffectTabNew.tsx             # 主组件
├── EffectTabNew.css             # 主组件样式
├── MetricCard.tsx               # KPI 指标卡片
├── MetricCard.css               # 卡片样式（玻璃态 + 光效）
├── ProgressSection.tsx          # 播放量达成进度区域
├── ProgressSection.css          # 进度样式（动态渐变）
├── useEffectData.ts             # 数据加载 Hook
├── useEffectCalculations.ts    # 数据计算 Hook
└── README.md                    # 本文档
```

---

## 🚀 核心功能

### 1. 时间维度切换 (T+7 / T+21)

- 使用 Ant Design Segmented 组件
- 仅支持 T+7 和 T+21 两个周期（**无 T+30**）
- 默认选中 T+21（主要验收节点）
- 切换时动态重新计算所有指标

### 2. KPI 指标卡片区

**4 个核心指标卡**:

1. **总播放量** - 蓝色主题 (#0ea5e9)
2. **实际 CPM** - 动态颜色（达标绿色 / 未达标红色）
3. **目标 CPM** - 靛蓝主题 (#6366f1)
4. **播放量达成率** - 动态颜色（含进度条）

**设计亮点**:
- 56x56 px 图标,采用深色渐变背景 + 白色图标
- 32px 超大数值,使用 SF Pro Display 字体 + 渐变色文本
- 悬停时卡片上浮 6px + 缩放 1.02 + 光效显现
- 每个卡片 0.1s 错开入场动画,营造节奏感

### 3. 播放量达成进度区域

**核心设计**:
- 16px 粗进度条,使用渐变色 + 动态光标
- 实时计算播放量 GAP（缺口分析）
- 根据达成率自动切换颜色主题:
  - ≥100%: 绿色渐变 + 奖杯徽章
  - 80-99%: 蓝色渐变
  - 60-79%: 黄色渐变
  - <60%: 红色渐变
- GAP 可视化小进度条,直观展示超出/不足量

### 4. 达人效果明细表格

**视图模式**:
- **全展开模式** (≥1440px 屏幕): 17 列完整数据
- **紧凑模式**: 10 列核心数据

**列定义** (全展开):
1. 达人昵称 (固定左侧)
2. 执行金额
3. 发布时间
4. 播放量
5. 总互动量
6. 点赞量
7. 评论量
8. 分享量
9. 互动率 (计算值)
10. 赞播比 (计算值)
11. **CPM** (带达标状态图标)
12. CPE
13. 组件展示
14. 组件点击
15. 组件点击率 (计算值)

**CPM 达标状态高亮**:
- CPM ≤ 基准值: 绿色粗体 + ✓ 图标
- CPM > 基准值: 红色 + ⚠ 图标

---

## 🧮 数据计算逻辑

### useEffectCalculations Hook

**项目级汇总统计**:

```typescript
interface EffectStats {
  totalViews: number;          // 总播放量
  actualCPM: number;            // 实际 CPM = (总成本 / 总播放量) * 1000
  targetViews: number;          // 目标播放量 = 总成本 / 基准CPM * 1000
  viewsGap: number;             // 播放量差值 = 实际 - 目标
  achievementRate: number;      // 达成率 = 实际 / 目标 * 100
  totalInteractions: number;    // 总互动量
  avgInteractionRate: number;   // 平均互动率
  avgLikeToViewRatio: number;   // 平均赞播比
}
```

**单条记录计算**:

```typescript
interface CollaborationMetrics {
  totalInteractions: number;    // likes + comments + shares
  interactionRate: number;      // (互动 / 播放) * 100
  likeToViewRatio: number;      // (点赞 / 播放) * 100
  cpm: number;                  // (成本 / 播放) * 1000
  cpe: number;                  // 成本 / 总互动量
  ctr: number;                  // (组件点击 / 组件展示) * 100
}
```

**关键公式**:
- 成本单位为"分",需除以 100 转为元
- CPM = (成本(元) / 播放量) * 1000
- CPE = 成本(元) / 总互动量

---

## 🎭 动画系统

### 关键帧动画

```css
@keyframes metric-card-float-in
@keyframes orb-pulse
@keyframes shimmer-slide
@keyframes progress-pulse
@keyframes cursor-pulse
@keyframes gap-bar-grow
```

### 缓动函数

统一使用 `cubic-bezier(0.34, 1.56, 0.64, 1)` (弹性缓动)

### 动画时序

- 指标卡片: 0s, 0.1s, 0.2s, 0.3s 错开入场
- 进度区域: 0.2s 延迟
- 表格区域: 0.4s 延迟

---

## 🎨 颜色体系

### 主题色

```css
--metric-blue: #3b82f6;      /* 播放量 */
--metric-pink: #ec4899;      /* 点赞 */
--metric-purple: #8b5cf6;    /* 评论 */
--metric-cyan: #06b6d4;      /* 分享 */
--metric-orange: #f97316;    /* CPM */
--metric-green: #10b981;     /* 达标 */
--metric-red: #ef4444;       /* 未达标 */
--metric-indigo: #6366f1;    /* AgentWorks 主色 */
--metric-amber: #f59e0b;     /* 警告 */
```

### 渐变配方

**成功状态**: `linear-gradient(135deg, #10b981 0%, #059669 100%)`
**警告状态**: `linear-gradient(135deg, #f59e0b 0%, #d97706 100%)`
**危险状态**: `linear-gradient(135deg, #f43f5e 0%, #e11d48 100%)`
**主题渐变**: `linear-gradient(135deg, #6366f1 0%, #4f46e5 100%)`

---

## 📱 响应式设计

### 断点

```css
xl: 1280px   /* 4 列卡片 + 全展开表格 */
lg: 1024px   /* 2 列卡片 */
md: 768px    /* 2 列卡片 */
sm: 640px    /* 1 列卡片 + 紧凑表格 */
```

### 自动视图切换

- ≥1440px: 默认全展开视图
- <1440px: 默认紧凑视图
- 用户可手动切换,优先级高于自动判断

---

## 🌙 暗色模式支持

通过 `@media (prefers-color-scheme: dark)` 自动适配:

- 卡片背景: 深色半透明玻璃态
- 文字颜色: 浅灰色 (#f1f5f9)
- 边框颜色: 半透明灰 (rgba(148, 163, 184, 0.2))
- 保持所有动画和渐变效果

---

## 🔧 使用方式

### 基础用法

```tsx
import { EffectTabNew } from './EffectTab';

<EffectTabNew
  projectId="proj_xxx"
  platforms={['douyin', 'kuaishou']}
  benchmarkCPM={10}
  onRefresh={() => console.log('Refreshed')}
/>
```

### Props

```typescript
interface EffectTabNewProps {
  projectId: string;           // 项目 ID
  platforms: Platform[];       // 平台列表（未使用,为向后兼容保留）
  benchmarkCPM?: number;       // 基准 CPM,默认 10
  onRefresh?: () => void;      // 刷新回调
}
```

---

## 🏆 设计亮点总结

1. **视觉冲击力**: 玻璃态 + 霓虹光效打破传统数据看板的沉闷
2. **信息层次**: 卡片 → 进度 → 表格,三级信息架构清晰
3. **状态可视化**: 颜色、图标、进度条多维度传达数据状态
4. **动效细节**: 每个交互都有精心设计的过渡动画
5. **性能优化**: 使用 CSS 动画而非 JS,GPU 加速,丝滑流畅
6. **可访问性**: 保持足够的颜色对比度,支持键盘导航

---

## 🔄 版本历史

- **v4.0.0** (2025-12): 全新玻璃态设计,仅支持 T+7/T+21
- **v3.0.0**: 动态指标配置
- **v2.0.0**: 实现动态列渲染
- **v1.0.0**: 初始硬编码版本

---

## 💡 后续优化方向

1. **图表可视化**: 播放量趋势图、互动量分布饼图
2. **对比分析**: T+7 vs T+21 双周期对比视图
3. **导出功能**: 支持导出 Excel/PDF 报告
4. **批量录入**: 支持批量导入效果数据
5. **智能建议**: 基于 AI 分析给出优化建议

---

## 🎯 设计原则

> "数据可视化不应该是冰冷的表格,而应该是能够讲故事的艺术品"

本设计严格遵循:
- **No Generic AI Slop**: 拒绝千篇一律的 Inter 字体 + 紫色渐变
- **Intentional Aesthetics**: 每个颜色、每个动画都有明确的设计意图
- **Performance First**: 优雅不以牺牲性能为代价
- **Context-Specific**: 为数据看板量身定制,而非套用通用模板

---

**设计 & 开发**: Claude Sonnet 4.5 + frontend-design skill
**设计工具**: Pure CSS + React Hooks
**灵感来源**: Glassmorphism, Cyberpunk 2077, Apple Design Language
