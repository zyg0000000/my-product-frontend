# 平台配置管理 - 产品功能指南

> **文档版本**: v1.1.0
>
> **创建时间**: 2025-11-23
>
> **最后更新**: 2025-11-29
>
> **维护者**: AgentWorks 团队
>
> **相关实施文档**: [PLATFORM_CONFIG_UNIFICATION_PLAN.md](./archive/PLATFORM_CONFIG_UNIFICATION_PLAN.md)

---

## 📚 文档目的

本文档用于：
1. 说明平台配置管理系统的产品功能
2. 解释功能开关的设计理念和判断标准
3. 指导未来新增功能的决策
4. 提供功能扩展的最佳实践

---

## 🎯 产品功能概述

### 平台配置管理系统

**访问路径**: 系统设置 → 平台配置管理 (`/settings/platform-config`)

**核心价值**:
- 统一管理所有平台的配置信息
- 支持动态调整，无需修改代码和重新部署
- 通过功能开关灵活控制平台能力
- 实现前后端配置完全统一

---

## 🏗️ 系统架构

### 三层架构

```
┌─────────────────────────────────────┐
│  数据层：MongoDB system_config 集合  │
│  - 存储平台配置                      │
│  - 功能开关状态                      │
│  - 业务参数                          │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│  服务层：platformConfigManager 云函数│
│  - RESTful API (GET/POST/PUT/DELETE)│
│  - 配置验证                          │
│  - 变更日志                          │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│  应用层：前端管理界面                 │
│  - 平台列表展示                      │
│  - 配置编辑                          │
│  - LocalStorage 缓存                 │
└─────────────────────────────────────┘
```

---

## 📋 配置项说明

### 1. 基础信息

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| platform | string | 平台唯一标识（创建后不可修改） | `douyin` |
| name | string | 平台中文名称 | `抖音` |
| enabled | boolean | 是否启用该平台 | `true` |
| color | string | 主题配色 | `blue` |
| order | number | 显示排序（数字越小越靠前） | `1` |

**说明：**
- `platform` 是系统级标识，创建后永久不变
- `enabled` 控制平台整体可用性（软删除机制）
- `color` 影响 Tag、按钮等 UI 元素的颜色

---

### 2. 账号ID配置

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| accountId.label | string | 表单标签文字 | `星图ID` |
| accountId.placeholder | string | 输入框占位符 | `请输入星图ID` |
| accountId.helpText | string | 帮助说明（可选） | `可在星图后台查看` |

**用途：**
- 不同平台的账号ID名称不同（星图ID、蒲公英ID等）
- 动态生成表单标签和提示文字
- 提升用户体验

---

### 3. 价格类型配置

```typescript
priceTypes: [
  {
    key: 'video_60plus',      // 价格类型标识
    label: '60s+',            // 显示标签
    required: true,           // 是否必填
    bgColor: '#dbeafe',       // 背景色
    textColor: '#1e40af',     // 文字色
    order: 1                  // 排序
  }
]
```

**说明：**
- 定义该平台支持的价格类型
- 不同平台的价格类型差异很大
- **当前不支持在界面中编辑**（需在数据库中修改）

**原因：**
- 数据结构复杂（6个字段）
- 与历史数据强关联，修改风险高
- 与 TypeScript 类型定义耦合
- 修改频率极低（每年 < 2次）

---

### 4. 业务配置

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| business.fee | number\|null | 平台费率（0.05 = 5%） | `0.05` |
| business.defaultRebate | number | 默认返点率 | `15` |
| business.minRebate | number | 最小返点率 | `0` |
| business.maxRebate | number | 最大返点率 | `100` |

**说明：**
- `fee` 为 `null` 表示该平台费率待确定
- 返点率范围用于表单验证

---

### 5. 外链配置（v2.0 多链接支持）

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| links | LinkConfig[] | 外链配置列表 | 见下方示例 |
| links[].name | string | 链接名称（内部标识） | `星图主页` |
| links[].label | string | 显示标签（限2个中文字） | `星图`, `抖音` |
| links[].template | string | URL 模板 | `https://xingtu.cn/...//{id}` |
| links[].idField | string | ID字段名 | `xingtuId` |

**示例配置：**
```javascript
links: [
  {
    name: '星图主页',
    label: '星图',
    template: 'https://www.xingtu.cn/ad/creator/author-homepage/douyin-video/{id}',
    idField: 'xingtuId'
  },
  {
    name: '抖音主页',
    label: '抖音',
    template: 'https://www.douyin.com/user/{id}',
    idField: 'douyinId'
  }
]
```

**说明：**
- 支持配置**不限数量**的外链
- 每个外链可使用不同的 ID 字段（如 xingtuId、douyinId）
- `{id}` 作为占位符，运行时替换为达人的实际ID
- `label` 字段限制为2个中文字符，用于在达人列表中显示
- `links` 为空数组 `[]` 表示该平台不支持外链
- **废弃字段**: 旧的 `link` 字段已废弃，但保留向后兼容

**UI 展示：**
- 达人名称旁边显示标签链接列表（如「星图」「抖音」）
- hover 标签显示完整链接名称
- 点击标签在新窗口打开对应页面

---

## 🎚️ 功能开关详解

### 功能开关的设计理念

**核心思想：**
- 不同平台支持的功能不同
- 新功能可以灰度发布
- 功能可以临时关闭（维护、调试）

**开关粒度原则：**
- ✅ 一个开关 = 一个功能模块
- ❌ 不要过细（如：查看价格、编辑价格分开）
- ❌ 不要过粗（如：所有达人功能合并）

---

### 当前的4个功能开关

#### 1. 价格管理（priceManagement）

**控制范围：**
- 达人价格数据的查看、编辑
- 价格历史记录
- 价格相关的统计和报表

**典型场景：**
- 抖音：有星图价格 → `true`
- B站：暂无价格数据 → `false`

**关闭后的表现：**
- 达人详情页不显示价格信息
- 价格管理功能隐藏或禁用
- 相关报表中该平台数据为空

---

#### 2. 返点管理（rebateManagement）

**控制范围：**
- 达人返点配置
- 机构返点同步
- 返点历史记录
- 返点调整

**典型场景：**
- 所有平台：都需要返点管理 → `true`
- 临时维护：可以关闭 → `false`

**关闭后的表现：**
- 返点管理按钮隐藏
- 返点弹窗无法打开
- 返点数据不显示

---

#### 3. 达人数据（performanceTracking）

**控制范围：**
- 达人表现数据追踪
- 粉丝数趋势
- 互动数据（点赞、评论、转发）
- 受众画像分析
- CPM 等指标

**典型场景：**
- 抖音：有完整数据源 → `true`
- B站：数据源未接入 → `false`

**关闭后的表现：**
- "近期表现"页面不显示该平台数据
- 或显示"暂无数据"提示

**对应页面：** `/performance` (近期表现)

---

#### 4. 数据导入（dataImport）

**控制范围：**
- 批量导入达人数据功能
- Excel/CSV 文件解析
- 数据验证和导入

**典型场景：**
- 抖音：支持从星图导出数据导入 → `true`
- 某平台：数据格式特殊，暂不支持 → `false`

**关闭后的表现：**
- 达人列表页的"导入数据"按钮隐藏
- 导入功能入口不显示

**对应组件：** `DataImportModal_v2.tsx`

---

## 🔍 功能开关判断标准

### 何时需要新增功能开关？

**满足以下任意条件即需要：**

#### 标准1：平台间有功能差异

```
✅ 需要开关：
- 直播带货：抖音支持，小红书不支持
- 短视频分析：抖音有，图文平台没有
- 特定API集成：仅部分平台可用

❌ 不需要开关：
- 达人列表查询：所有平台都需要
- 达人基本信息：所有平台都有
- 搜索功能：通用功能
```

#### 标准2：功能可能被临时关闭

```
✅ 需要开关：
- 依赖外部API：可能不稳定，需要能快速关闭
- 增值功能：按需启用
- 实验性功能：测试期可能关闭

❌ 不需要开关：
- 核心必备功能：关闭会导致系统不可用
```

#### 标准3：需要灰度发布

```
✅ 需要开关：
- 新功能先在抖音试点
- 稳定后再推广到其他平台

❌ 不需要开关：
- 同时上线所有平台的功能
```

---

### 快速判断法（3个问题）

**问自己：**

1. **所有平台都必须有这个功能吗？**
   - 否 → 需要开关 ✅
   - 是 → 继续问题2

2. **这个功能可能会被临时关闭吗？**
   - 是 → 需要开关 ✅
   - 否 → 继续问题3

3. **关闭这个功能，系统还能正常运行吗？**
   - 是 → 需要开关 ✅
   - 否 → 不需要开关（核心功能）❌

---

### 不需要功能开关的场景

**明确不需要开关的情况：**

#### 1. 平台选择本身
```
❌ 不需要：
- 平台 Tabs (抖音、小红书、B站、快手)
- 平台下拉选择
- 平台筛选器

理由：这是数据维度，不是功能
```

#### 2. 核心基础功能
```
❌ 不需要：
- 达人列表查询
- 达人详情查看
- 达人创建/编辑/删除
- 达人搜索
- 基本信息展示

理由：所有平台都必须支持
```

#### 3. UI/UX 层面
```
❌ 不需要：
- 界面主题
- 列表排序
- 分页
- 筛选器

理由：与平台业务逻辑无关
```

#### 4. 技术实现细节
```
❌ 不需要：
- 数据缓存
- 请求重试
- 错误处理
- 日志记录

理由：用户不感知的技术细节
```

---

## 📐 功能开关命名规范

### 命名规则

**格式：** `功能名称` + `Management/Tracking/Import` 等后缀

**示例：**
- ✅ `priceManagement` - 价格管理
- ✅ `performanceTracking` - 表现追踪
- ✅ `dataImport` - 数据导入
- ✅ `liveStreaming` - 直播带货
- ✅ `contractManagement` - 合同管理

**不好的命名：**
- ❌ `price` - 太简略，不清楚是什么
- ❌ `douyin_price` - 不应该包含平台名
- ❌ `canManagePrice` - 冗余的 can 前缀
- ❌ `支持价格管理` - 应该用英文

---

## 🚀 新增功能流程

### 完整的5步流程（以"直播带货"为例）

#### 步骤1：判断是否需要功能开关

**问题检查：**
- [ ] 所有平台都支持吗？（否 → 需要）
- [ ] 可能被临时关闭吗？（是 → 需要）
- [ ] 关闭后系统仍可用吗？（是 → 需要）

**决定：** 抖音支持直播，小红书不支持 → **需要开关** ✅

#### 步骤2：在数据库中添加开关字段

```javascript
// MongoDB Shell
use agentworks_db

// 给支持的平台开启
db.system_config.updateOne(
  { configType: 'platform', platform: 'douyin' },
  { $set: { 'features.liveStreaming': true } }
);

// 给不支持的平台关闭
db.system_config.updateMany(
  {
    configType: 'platform',
    platform: { $in: ['xiaohongshu', 'bilibili', 'kuaishou'] }
  },
  { $set: { 'features.liveStreaming': false } }
);
```

#### 步骤3：更新 TypeScript 类型

**文件：** `frontends/agentworks/src/api/platformConfig.ts`

```typescript
export interface PlatformConfig {
  // ... 其他字段
  features: {
    priceManagement: boolean;
    performanceTracking: boolean;
    rebateManagement: boolean;
    dataImport: boolean;
    liveStreaming?: boolean;        // ← 新增（可选字段）
  };
}
```

#### 步骤4：在编辑界面添加开关

**文件：** `frontends/agentworks/src/components/PlatformConfigModal.tsx`

**4.1 在表单初始化中添加默认值：**
```typescript
// 新增模式
form.setFieldsValue({
  // ... 其他字段
  liveStreaming: false,  // ← 新增
});

// 编辑模式
form.setFieldsValue({
  // ... 其他字段
  liveStreaming: config.features?.liveStreaming,  // ← 新增
});
```

**4.2 在业务配置 Tab 添加开关控件：**
```tsx
<Form.Item
  name="liveStreaming"
  label="直播带货"
  valuePropName="checked"
  extra="是否支持该平台的直播带货功能"
>
  <Switch checkedChildren="启用" unCheckedChildren="禁用" />
</Form.Item>
```

**4.3 在表单提交时包含：**
```typescript
features: {
  // ... 其他字段
  liveStreaming: values.liveStreaming ?? false,  // ← 新增
}
```

#### 步骤5：在列表中显示

**文件：** `frontends/agentworks/src/pages/Settings/PlatformConfig.tsx`

**功能支持列添加标签：**
```tsx
<Space size={4} wrap={false}>
  {record.features?.priceManagement && <Tag color="blue">价格管理</Tag>}
  {record.features?.rebateManagement && <Tag color="green">返点管理</Tag>}
  {record.features?.performanceTracking && <Tag color="purple">达人数据</Tag>}
  {record.features?.dataImport && <Tag color="orange">数据导入</Tag>}
  {record.features?.liveStreaming && <Tag color="red">直播带货</Tag>}  {/* ← 新增 */}
</Space>
```

#### 步骤6：在业务代码中使用

**在需要的页面中：**
```typescript
import { usePlatformConfig } from '../hooks/usePlatformConfig';

function LiveStreamingPage() {
  const { getPlatformConfigByKey } = usePlatformConfig();
  const config = getPlatformConfigByKey(selectedPlatform);

  // 根据开关决定功能可用性
  if (!config?.features.liveStreaming) {
    return <EmptyState message="该平台暂不支持直播带货功能" />;
  }

  return <LiveStreamingContent />;
}
```

**或者：**
```tsx
// 根据开关决定是否显示功能入口
{platformConfig?.features.liveStreaming && (
  <Button onClick={openLiveStreaming}>
    直播数据分析
  </Button>
)}
```

---

## 📊 当前功能开关清单

### 已实现的功能开关

| 开关Key | 显示标签 | 颜色 | 默认值 | 说明 |
|---------|---------|------|--------|------|
| `priceManagement` | 价格管理 | blue | 抖音/小红书: true<br>B站/快手: false | 价格数据的增删改查 |
| `rebateManagement` | 返点管理 | green | 全部: true | 返点配置和历史 |
| `performanceTracking` | 达人数据 | purple | 抖音/小红书: true<br>B站/快手: false | 表现数据追踪分析 |
| `dataImport` | 数据导入 | orange | 全部: true | 批量导入达人数据 |

### 未来可能的功能开关

| 开关Key | 显示标签 | 适用场景 | 优先级 |
|---------|---------|---------|--------|
| `liveStreaming` | 直播带货 | 直播数据分析 | 中 |
| `contractManagement` | 合同管理 | 电子合同管理 | 低 |
| `aiRecommendation` | AI推荐 | 智能推荐达人 | 低 |
| `advancedAnalytics` | 高级分析 | 深度数据分析 | 低 |
| `apiIntegration` | API集成 | 官方API对接 | 中 |
| `autoPublish` | 自动发布 | 内容自动发布 | 低 |
| `cooperationTracking` | 合作追踪 | 合作历史管理 | 中 |

---

## 🎯 开关管理最佳实践

### 1. 开关数量控制

**推荐范围：** 5-10个

**原因：**
- 太少（< 3个）：无法体现平台差异
- 太多（> 15个）：管理复杂，界面臃肿

**当前状态：** 4个 ✅（合理范围）

---

### 2. 开关的生命周期

#### 新功能上线

```
Phase 1: 实验期
├─ 抖音: liveStreaming = true  (试点)
└─ 其他: liveStreaming = false (观察)

Phase 2: 推广期
├─ 抖音: true  (稳定)
├─ 小红书: true (新增)
└─ 其他: false

Phase 3: 成熟期
└─ 全平台: true
```

#### 功能下线

```
不要删除开关，只设置为 false：
- 保留配置结构
- 便于未来重新启用
- 历史数据不受影响
```

---

### 3. 开关状态设计

**推荐：** 布尔值（true/false）

**不推荐：** 三态或多态
```typescript
// ❌ 不要这样设计
features: {
  liveStreaming: 'enabled' | 'disabled' | 'beta' | 'deprecated'
}

// ✅ 简单的布尔值
features: {
  liveStreaming: boolean
}
```

**如果需要状态信息，单独字段：**
```typescript
features: {
  liveStreaming: true  // 是否启用
},
featureStatus: {
  liveStreaming: 'beta'  // 功能状态
}
```

---

## 🔄 未来演进方案

### 当前方案：硬编码开关（Phase 4已实现）

**特点：**
- 开关在代码中硬编码
- 新增开关需要修改3-5处代码
- 类型安全

**适用场景：**
- 功能开关 < 7个
- 新增频率 < 每月1次

---

### 中期方案：配置文件驱动

**实施时机：** 功能开关达到7-8个时

**改造内容：**
```typescript
// src/config/featureDefinitions.ts
export const FEATURE_DEFINITIONS = [
  {
    key: 'priceManagement',
    label: '价格管理',
    color: 'blue',
    description: '...'
  },
  // ... 其他功能
];

// 界面动态渲染
{FEATURE_DEFINITIONS.map(feature => (
  <Form.Item key={feature.key} name={feature.key}>
    <Switch />
  </Form.Item>
))}
```

**优点：**
- 新增功能只改1个文件
- 保持类型安全
- 配置集中管理

**成本：** 2-3小时

---

### 长期方案：元数据驱动

**实施时机：** 功能开关 > 15个，或需要运营人员管理

**改造内容：**
- 在数据库创建 `feature_definitions` 集合
- 开发功能定义管理界面
- 完全动态化

**优点：**
- 新增功能零代码修改
- 完全界面化管理

**成本：** 4-6小时

---

## 📝 使用示例

### 示例1：在业务代码中检查功能开关

```typescript
// 方式1：早期返回
function PriceManagementPage({ platform }: Props) {
  const { getPlatformConfigByKey } = usePlatformConfig();
  const config = getPlatformConfigByKey(platform);

  if (!config?.features.priceManagement) {
    return (
      <EmptyState
        icon={<LockOutlined />}
        title="功能未开启"
        description="该平台暂不支持价格管理功能"
      />
    );
  }

  return <PriceManagementContent />;
}

// 方式2：条件渲染
function TalentDetail({ talent }: Props) {
  const { getPlatformConfigByKey } = usePlatformConfig();
  const config = getPlatformConfigByKey(talent.platform);

  return (
    <div>
      {/* 基本信息 - 所有平台都显示 */}
      <BasicInfo talent={talent} />

      {/* 价格信息 - 根据开关显示 */}
      {config?.features.priceManagement && (
        <PriceSection talent={talent} />
      )}

      {/* 返点信息 - 根据开关显示 */}
      {config?.features.rebateManagement && (
        <RebateSection talent={talent} />
      )}
    </div>
  );
}
```

---

### 示例2：在列表中根据开关显示操作按钮

```typescript
function TalentListActions({ talent }: Props) {
  const { getPlatformConfigByKey } = usePlatformConfig();
  const config = getPlatformConfigByKey(talent.platform);

  return (
    <Space>
      <Button icon={<EditOutlined />}>编辑</Button>

      {config?.features.priceManagement && (
        <Button icon={<DollarOutlined />}>价格</Button>
      )}

      {config?.features.rebateManagement && (
        <Button icon={<PercentageOutlined />}>返点</Button>
      )}

      {config?.features.liveStreaming && (
        <Button icon={<PlayCircleOutlined />}>直播</Button>
      )}
    </Space>
  );
}
```

---

## ⚠️ 注意事项

### 1. 开关与权限的区别

**功能开关（Platform Feature）：**
- 控制：平台是否支持某功能
- 场景：技术限制、业务差异
- 示例：B站不支持价格管理

**用户权限（User Permission）：**
- 控制：用户是否有权限操作
- 场景：角色权限、访问控制
- 示例：只有管理员能修改价格

**两者需要同时满足：**
```typescript
// 正确的权限检查
const canManagePrice =
  platformConfig.features.priceManagement &&  // 平台支持
  userPermissions.includes('price:edit');     // 用户有权限
```

---

### 2. 开关的向后兼容

**新增开关时：**
- 使用可选字段 `liveStreaming?: boolean`
- 提供默认值处理 `features.liveStreaming ?? false`
- 不影响现有平台配置

**示例：**
```typescript
// 读取时安全处理
const hasLiveStreaming = config.features?.liveStreaming ?? false;
```

---

### 3. 开关状态的缓存

**当前机制：**
- 平台配置缓存在 LocalStorage（24小时）
- 修改后需要刷新缓存

**刷新方法：**
1. 点击"刷新配置"按钮
2. 清除浏览器缓存
3. 代码中调用 `refreshConfigs()`

---

## 📚 相关文档

### 技术文档
- [平台配置统一方案](./PLATFORM_CONFIG_UNIFICATION_PLAN.md) - 整体架构设计
- [云函数改造清单](./CLOUD_FUNCTIONS_PLATFORM_MIGRATION_CHECKLIST.md) - 后端改造计划
- [UI/UX 规范](../UI_UX_GUIDELINES.md) - 界面设计规范

### 数据库文档
- 初始化脚本：`database/agentworks_db/scripts/init-platform-config.js`
- 数据结构：参考 `PLATFORM_CONFIG_UNIFICATION_PLAN.md`

### API 文档
- 云函数 README：`functions/platformConfigManager/README.md`
- API 接口：`frontends/agentworks/src/api/platformConfig.ts`

---

## 💡 常见问题 FAQ

### Q1: 如何决定一个新功能是否需要开关？

**A:** 用3个问题快速判断：
1. 所有平台都必须有吗？（否 → 需要）
2. 可能被临时关闭吗？（是 → 需要）
3. 关闭后系统仍可用吗？（是 → 需要）

---

### Q2: 平台选择器需要配置吗？

**A:** 不需要功能开关。

平台选择是**数据维度**，不是功能。系统通过 `enabled` 字段控制哪些平台可选择。

---

### Q3: 如何给B站新增价格管理功能？

**A:**
1. 在平台配置管理页面
2. 编辑B站配置
3. 进入"业务配置" Tab
4. 开启"价格管理"开关
5. 保存

立即生效，无需重新部署。

---

### Q4: 功能开关会越来越多吗？

**A:** 不会无限增长。

**原因：**
- 大部分功能是所有平台通用的
- 只有"平台间有差异"的功能才需要开关
- 预计稳定在 5-10 个

---

### Q5: 价格类型为什么不能在界面编辑？

**A:** 因为：
- 数据结构复杂（6个字段）
- 与历史数据强关联，修改风险高
- 与 TypeScript 类型耦合
- 修改频率极低

**建议：** 在数据库中直接修改，或联系开发者。

---

## 🎯 关键设计决策记录

### 决策1：功能开关存储位置

**选择：** 存储在平台配置的 `features` 字段中

**理由：**
- 与平台强关联
- 查询效率高（一次查询获取所有信息）
- 数据结构清晰

**替代方案：** 单独的 `platform_features` 集合
**未采用原因：** 增加查询复杂度，收益不明显

---

### 决策2：功能开关的界面形式

**选择：** Switch 开关

**理由：**
- 直观表达开/关状态
- 符合用户习惯
- Ant Design 标准组件

**替代方案：** Radio、Checkbox
**未采用原因：** 不如 Switch 直观

---

### 决策3：功能开关的命名语言

**选择：** 英文（camelCase）

**理由：**
- 符合编程规范
- TypeScript 类型定义友好
- 便于代码中使用

**显示：** 中文标签在界面上展示

---

## 📅 版本历史

### v1.1.0 (2025-11-29)

**外链配置多链接支持：**
- 外链配置从单链接扩展为支持**不限数量**的多链接
- 新增 `label` 字段（显示标签，限2个中文字）替代原有的 `icon` 字段
- 使用 ProFormList 实现动态添加/删除外链配置
- 达人列表中外链以标签形式显示在名称旁边
- 优化外链配置 UI 布局（4列网格布局、卡片包裹、实时预览）

**配置字段变更：**
- `links[].name`: 链接名称（内部标识）
- `links[].label`: 显示标签（限2个中文字，如「星图」「抖音」）
- `links[].template`: URL模板（使用 `{id}` 占位符）
- `links[].idField`: ID字段名

### v1.0.0 (2025-11-23)

**初始版本：**
- 实现4个基础功能开关
- 建立开关判断标准
- 制定命名规范
- 文档化最佳实践

**已实现功能：**
- ✅ 平台配置管理界面
- ✅ 功能开关编辑
- ✅ 动态功能控制
- ✅ LocalStorage 缓存

---

## 🔮 未来规划

### 短期（3个月内）

- [ ] 观察功能开关使用情况
- [ ] 收集新功能需求
- [ ] 评估是否需要新增开关

### 中期（6-12个月）

- [ ] 如果开关数量 > 7个，考虑升级到配置文件方案
- [ ] 优化开关管理界面（如：分类展示）
- [ ] 添加开关使用统计

### 长期（1年后）

- [ ] 如果开关数量 > 15个，考虑元数据驱动方案
- [ ] 开发功能定义管理界面
- [ ] 实现完全动态化

---

**文档维护**: 每次新增功能开关时更新本文档
**最后更新**: 2025-11-29
**维护者**: AgentWorks 团队
